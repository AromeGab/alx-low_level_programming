#include <stdio.h>

/**
 * main - Program to produce the alphabets in lowercase
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char x;

	for (x = 'a'; x <= 'z'; c++)
	{
		putchar(x);
	}
	puchar('\n');
	return (0);
}
